import { Inter } from "next/font/google";
import { Lato } from "next/font/google";
export const inter = Inter({ subsets: ["latin"] });
export const lato = Lato({ weight: ["400", "900", "700"], subsets: ["latin"] });
